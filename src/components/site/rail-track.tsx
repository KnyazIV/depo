/**
 * Рельсовый путь.
 *
 * Не фон страницы, а полоса в вёрстке. Фоном он не работал: контент заполняет
 * экран целиком, и путь всегда оказывался под панелями. Здесь у него есть
 * своё место — воздух между героем и табло, где его видно.
 *
 * Полоса выходит за колонку текста на всю ширину экрана: путь должен уходить
 * за края, иначе он читается как отрезок, а не как дорога.
 */
export function RailTrack() {
  return (
    <div aria-hidden="true" className="rail-track relative left-1/2 w-screen -translate-x-1/2" />
  );
}
